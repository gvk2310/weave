apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcchart.fullname" . }}-test-connection"
  labels:
    {{- include "mcchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
