---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: devnetops-onboard
  labels:
    app: devnetops-onboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devnetops-onboard
  template:
    metadata:
      labels:
        app: devnetops-onboard
    spec:
      imagePullSecrets:
      - name: devnetops
      containers:
      - name: devnetops-onboard
        image: devnetops.azurecr.io/devnetops-onboard:t1
        imagePullPolicy: Always
        env:
        - name: vaultToken
          value: devnetops
        - name: vaultUrl
          value: http://vault1.ethan.svc.cluster.local:80
        - name: usermgmtUrl
          value: http://devnetops-access.ethan.svc.cluster.local:80
        - name: mongohost
          value: mongodb://admin:password@mongo-devnetops.ethan.svc.cluster.local:80/devnetops?authSource=admin
        - name: redis_url
          value: redis-ls.ethan.svc.cluster.local:6378
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: devnetops-onboard
  labels:
    app: devnetops-onboard
spec:
  selector:
    app: devnetops-onboard
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
---

